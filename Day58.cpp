// 50. Pow(x, n)
//     Implement pow(x, n),
//     which calculates x raised to the power n(i.e., xn).

class Solution
{
public:
    double myPow(double x, int n)
    {
        double ans = pow(x, n);

        return ans;
    }
};